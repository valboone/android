<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/trailerImage"
        android:layout_width="match_parent"
        android:layout_height="244dp"
        android:layout_alignParentTop="true"
        android:layout_marginTop="38dp"
        app:srcCompat="?attr/colorButtonNormal" />

    <TextView
        android:id="@+id/description"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/trailerImage"
        android:layout_marginTop="9dp"
        android:text="TextView" />

    <ImageButton
        android:id="@+id/playButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignStart="@+id/trailerImage"
        android:layout_alignTop="@+id/trailerImage"
        android:layout_alignBottom="@+id/trailerImage"
        android:layout_alignEnd="@+id/trailerImage"
        android:layout_marginTop="0dp"
        android:layout_marginBottom="0dp"
        android:background="@null"
        android:foregroundGravity="center_horizontal"
        app:srcCompat="@android:drawable/ic_media_play"
        android:layout_alignLeft="@+id/trailerImage"
        android:layout_alignRight="@+id/trailerImage"
        android:onClick="launch"/>
</RelativeLayout>